TODO:
* Start writing docs
* Add Entity init tests
* Add rendering functions
* Figure out how to improve performance of EntityPath (parsing, validation, etc.)
* Add EntUi.getState() and EntUi.setState()
* Generalize EntityPath for use with state fetching as well